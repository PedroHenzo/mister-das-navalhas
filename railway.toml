[build]
builder = "NIXPACKS"
buildCommand = "npm install && npx puppeteer browsers install chrome"

[deploy]
startCommand = "node server.js"
healthcheckPath = "/api/barbers"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
